name: 自动更新IPTV数据
 on:
   schedule:
     # 每天凌晨2点（UTC时间）自动运行
     - cron: '0 2 * * *'
   workflow_dispatch:
     # 允许在GitHub页面上手动触发运行
 jobs:
   update:
     runs-on: ubuntu-latest
     steps:
       - name: 克隆仓库到服务器
         uses: actions/checkout@v4
         with:
           fetch-depth: 0
       - name: 设置Python环境
         uses: actions/setup-python@v5
         with:
           python-version: '3.10'
       - name: 安装脚本依赖
         run: |
           python -m pip install --upgrade pip
           pip install requests  # 如果你的 fofa_fetch.py 需要 requests 库
       - name: 运行更新脚本
         run: python fofa_fetch.py
       - name: 自动提交并推送更新
         uses: EndBug/add-and-commit@v9
         with:
           message: '🎉 自动更新IPTV数据'
           committer_name: GitHub Actions Bot
           committer_email: actions@github.com
           add: '["IPTV.txt", "统计.txt"]'
